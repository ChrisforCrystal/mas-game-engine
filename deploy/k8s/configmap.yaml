apiVersion: v1
kind: ConfigMap
metadata:
  name: arena-config
  namespace: arena
data:
  # 前端调用 API 的地址，改成你实际暴露的域名或 NodePort IP
  NEXT_PUBLIC_ARENA_API: "http://arena-api:9090"
  # maps 目录路径（容器内）
  ARENA_MAPS_DIR: "/app/maps"
